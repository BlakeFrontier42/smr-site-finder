import type { Metadata } from "next";
import { Inter } from "next/font/google";
import { Analytics } from "@vercel/analytics/next";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "SMR Site Finder — Where Should America Build Its Next Nuclear Reactors?",
  description: "Interactive map of optimal Small Modular Reactor deployment sites based on DOE data, retiring coal plants, grid demand, water access, and seismic risk.",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        {children}
        <Analytics />
      </body>
    </html>
  );
}
